# Vite Environment Variables
# 모든 변수는 VITE_ 접두사가 필요합니다

# API 서버 URL
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Mapbox Access Token
VITE_MAPBOX_ACCESS_TOKEN=your-mapbox-access-token

# Mock 데이터 사용 여부 (true: mock 데이터, false 또는 미설정: 실제 API)
# TMap API 일일 호출 제한(10회)으로 인해 개발 시 true로 설정 권장
VITE_USE_MOCK_DATA=true
